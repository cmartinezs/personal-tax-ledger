# Tests de integración

Suite global ejecutada por `npm test`. Incluye contratos HTTP, arquitectura, casos de uso, cálculos, adaptadores, frontend estático y lifecycle local.

Los tests de unidad de paquetes viven junto a cada workspace; esta carpeta es para integración entre límites. Consulta [`../../packages/README.md`](../../packages/README.md) para encontrarlos.
