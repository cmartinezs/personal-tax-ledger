# Tests de contracts

Validan `WorkspaceContext`, métodos obligatorios y fallos de contratos de repositorio. Los dobles deben ser mínimos para no ocultar una API incompleta.

Ejecutar: `npm test --workspace @personal-tax-ledger/contracts`.
