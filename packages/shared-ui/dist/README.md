# Artefacto compilado

Esta carpeta contiene el JavaScript y las declaraciones TypeScript que expone `@personal-tax-ledger/shared-ui`. Se versiona para que `web` y consumidores externos funcionen sin compilar JSX/TSX previamente.

No editar manualmente. Regenerar con `npm run build --workspace @personal-tax-ledger/shared-ui`.
