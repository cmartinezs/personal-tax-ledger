# src

Contenido del inbound adapter HTTP reusable.

- `http-errors.mjs`: respuesta JSON, errores de API y mapeo de `ValidationError`.
- `read-json-body.mjs`: lectura segura del cuerpo de petición.
- `query-params.mjs`: parsing de parámetros de query.
- `index.mjs`: superficie pública del package.
