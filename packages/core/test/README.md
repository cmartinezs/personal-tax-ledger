# Tests de core

Prueban cálculos puros y la API pública del motor tributario. No deben importar SQLite, HTTP, React, filesystem de aplicación ni casos de uso.

Ejecutar: `npm test --workspace @personal-tax-ledger/core`.
