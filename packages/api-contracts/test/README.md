# Tests de API contracts

Prueban normalización de requests, responses, filtros, errores y paginación. Son independientes de `web` y `server`.

Ejecutar: `npm test --workspace @personal-tax-ledger/api-contracts`.
