# Tests de application

Prueban casos de uso con repositorios inyectados. No deben conocer HTTP, SQLite ni React; cada test debe verificar contexto y coordinación observable.

Ejecutar: `npm test --workspace @personal-tax-ledger/application`.
